# Vite environment variables must be prefixed with VITE_
# If you bypass the proxy and call the backend directly, set this:
VITE_API_BASE=http://localhost:5040

# Example: override default proxy usage in code
# (Ensure your code reads from import.meta.env.VITE_API_BASE when needed)
